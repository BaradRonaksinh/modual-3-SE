/* 4. WAP to print table up to given numbers.*/

#include<stdio.h>

int main(){
	int i, n, table;
	printf("Table Number:");
	scanf("%d", &n);
	table = i * n;
	
	for(i = 1; i <= 10; i++){
		printf("\n%d * %d = %d", n, i, table= i * n);
	}
}
